import { FunctionComponent } from 'react';

import { SlotProps } from '../../types';

export const HeaderButtons: FunctionComponent<SlotProps> = ({ children }) => children;
export const HeaderAvatar: FunctionComponent<SlotProps> = ({ children }) => children;
export const HeaderTags: FunctionComponent<SlotProps> = ({ children }) => children;
